import 'package:flutter/material.dart';

class AdspaceScreen extends StatelessWidget {
  const AdspaceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Buy Adspace')),
      body: const Center(
        child: Text('Adspace page coming soon.'),
      ),
    );
  }
}
